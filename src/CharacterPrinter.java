public class CharacterPrinter {
    public static void main(String[] args) {
        //String: series of character
        //Int: any whole number (neg, 0, pos)
        //Double: numbers with decimal/fraction
        //Variable: can store values
        //Boolean = true/false option only 2 values
       String x;
       int age = 20 + 4;
        double height = 68.1;

        age = age + 16;
        age += 16;
        age -= 8;
        age += 4;
        age /= 2;

        age = age + 1;  // age = age - 1
        age += 1;       // age -= 1
        age++;          // age--;

        x = "Hi";

        System.out.println("My age is" + age);
        System.out.println(x);
        System.out.println(x);
    }
}
